{
  "name": "webgl-uprunning",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "parcel-bundler": "^1.7.0",
    "parcel-plugin-shader-loader": "^1.0.0",
    "three": "^0.90.0"
  },
  "devDependencies": {
    "babel-core": "^6.26.0"
	},
	"scripts": {
		"dev": "rm -rf dist && rm -rf .cache && parcel index.html --no-hmr --no-source-maps"
	}
}
